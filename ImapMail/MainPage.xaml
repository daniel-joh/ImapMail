<Page
    x:Class="ImapMail.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImapMail"
    xmlns:mailkit="using:MailKit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   
    mc:Ignorable="d">


    <Page.TopAppBar>
        <CommandBar>
            <AppBarButton Name="AppBarButtonRefresh" Icon="Refresh" Label="Refresh" Click="AppBarButton_Click"></AppBarButton>
            <AppBarButton Name="AppBarButtonCompose" Icon="Add" Label="Compose new mail" Click="AppBarButton_Click"></AppBarButton>
            <AppBarButton Name="AppBarButtonSettings" Icon="Setting" Label="Settings" Click="AppBarButton_Click"></AppBarButton>
        </CommandBar>
    </Page.TopAppBar>

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Orientation="Horizontal">


        <ListView Name="MailListView" Width="550" ItemsSource="{x:Bind SummaryList, Mode=OneWay}" Margin="20,20,20,20" 
                  ItemClick="MailListView_ItemClicked" IsItemClickEnabled="true" SelectionMode="none">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="mailkit:MessageSummary">
                    <StackPanel Orientation="Vertical" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Name="Sender" FontSize="16"  Text="{x:Bind Envelope.From}" FontWeight="Bold" ></TextBlock>
                            <TextBlock Name="Date" FontSize="16" Margin="20,0,0,0" Text="{x:Bind Envelope.Date}" TextAlignment="Right"></TextBlock>
                        </StackPanel>
                        <TextBlock Name="Subject" FontSize="16" Text="{x:Bind Envelope.Subject}" TextTrimming="Clip"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
                
        <WebView x:Name="webView" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="0,0,100,20" Width="700" MinWidth="495" ScrollViewer.VerticalScrollBarVisibility="Auto" />

    </StackPanel>

</Page>

