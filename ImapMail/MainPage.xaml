<Page
    x:Class="ImapMail.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:ImapMail"
    mc:Ignorable="d">

    <Page.TopAppBar>
        <CommandBar Background="CornflowerBlue" >
            <AppBarButton Name="AppBarButtonRefresh" Icon="Refresh" Label="Refresh" Click="AppBarButton_Click"></AppBarButton>
            <AppBarButton Name="AppBarButtonCompose" Icon="Mail" Label="Compose new mail" Click="AppBarButton_Click"></AppBarButton>
            <AppBarButton Name="AppBarButtonSettings" Icon="Setting" Label="Settings" Click="AppBarButton_Click"></AppBarButton>
        </CommandBar>
    </Page.TopAppBar>

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Orientation="Horizontal">

        <ListView Name="MailListView" Width="450" ItemsSource="{x:Bind MailHeaderList, Mode=OneWay}" Margin="20,20,20,20" 
                  ItemClick="MailListView_ItemClicked" IsItemClickEnabled="true" SelectionMode="none" BorderBrush="Transparent" BorderThickness="0.5">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:MailHeader">
                    <StackPanel Orientation="Vertical" Margin="0,0,0,25" >
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Name="Sender" FontSize="16"  Text="{x:Bind From}" FontWeight="Bold" ></TextBlock>
                            <TextBlock Name="Date" FontSize="16" Margin="15,0,0,0" Text="{x:Bind Date}" TextAlignment="Right"></TextBlock>
                        </StackPanel>
                        <TextBlock Name="Subject" FontSize="16" Text="{x:Bind Subject}" TextTrimming="CharacterEllipsis"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Orientation="Horizontal" BorderBrush="Black" BorderThickness="1" Margin="20,20,20,20">
          
            <WebView x:Name="webView" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Margin="20,20,20,20" Width="700" MinWidth="500" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </StackPanel>
    </StackPanel>

</Page>

